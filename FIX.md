너는 gyeomyi/hanroro 레포에서 아래 문제들을 실제로 재현·검증하면서 고친다. 
"고쳤다"는 보고가 실제 동작과 어긋난 전례가 있으니, 이번엔 코드만 고치고 
끝내지 말고 CLAUDE.md에 있는 헤드리스 크롬 검증 방법으로 매번 직접 눌러서 
확인한 뒤에만 완료로 표시해라. 나는 개입하지 않는다. 끝까지 스스로 판단해서 완료해라.

## 0. 먼저 할 일
1. git status, git log로 로컬에만 있고 원격(https://github.com/gyeomyi/hanroro)에는 
   안 올라간 커밋·변경사항이 있는지 확인해라. 있으면 브랜치/충돌 상태를 파악해라.
2. 아래 각 항목을 고치기 전에, 먼저 헤드리스 크롬으로 현재 상태를 재현해서 
   "정확히 뭐가 안 되는지" 스크린샷/콘솔 로그로 확인해라. 짐작으로 고치지 마라.

## 1. 유튜브 재생 안 되는 문제
- 실제로 페이지를 띄우고 재생 버튼을 눌러서 재생이 되는지, 콘솔에 에러가 뜨는지, 
  네트워크 요청이 막히는지 직접 확인해라.
- 파사드(썸네일 클릭 → iframe 삽입) 방식이라면 iframe 생성 시점, youtube-nocookie 
  도메인, embed 파라미터, CSP/오토플레이 정책 문제인지 각각 체크해라.
- 고친 뒤 반드시 다시 헤드리스 크롬으로 클릭 → 실제 재생 확인까지 마쳐라. 
  "코드상 맞는 것 같다"는 완료 기준이 아니다.

## 2. 방명록(guestbook) 수정 기능이 제대로 안 됨
- 실제로 글을 하나 작성하고, 비밀번호로 수정을 시도해서 어느 지점에서 실패하는지 
  재현해라 (프론트 JS 문제인지, edit_guestbook RPC 응답 문제인지, 
  Supabase 권한/정책 문제인지).
- CLAUDE.md의 보안 구조(컬럼 단위 SELECT, RPC, 브루트포스 제한)를 깨지 않는 
  선에서 버그만 고쳐라. 구조 자체를 새로 짜지 마라.
- 고친 뒤 실제로 작성→수정→결과 확인까지 전 과정을 다시 테스트해라.

## 3. 음악 플랫폼을 요즘 가장 많이 쓰는 것 기준으로
- 지금은 스포티파이/벅스/애플뮤직 위주로 돼 있는데, 한로로의 실제 팬층(국내 Z세대) 
  이 요즘 가장 많이 쓰는 스트리밍 플랫폼이 뭔지 조사해서 주력 플랫폼을 정해라 
  (예: 유튜브 뮤직, 스포티파이, 멜론 등 — 임의로 정하지 말고 근거를 남겨라).
- 정한 플랫폼을 "듣기" 섹션의 기본/우선 노출로 삼고, 나머지는 보조 링크로 유지해라.
- 실제 링크가 유효한지(아티스트 페이지가 맞는지) 열어서 확인해라.

## 4. 어색한 문장 표현을 트렌디하게
- 사이트 전체 카피를 다시 훑어서 어색하거나 낡은 표현을 다듬어라.
- 단, "한 권의 책" 컨셉과 문학적 톤(CLAUDE.md 디자인 컨셉 참고)은 유지해라 — 
  트렌디하게 만든다고 인터넷 밈체·과한 신조어로 가지 마라. 세련되고 자연스럽게, 
  격은 지키되 촌스럽지 않게가 기준이다.
- 바꾼 문장은 DECISIONS.md나 별도 로그에 전/후 비교로 남겨라.

## 5. 커밋·푸시 정리
- 위 수정사항을 포함해서, 로컬에만 있고 GitHub 원격에 반영 안 된 모든 변경을 
  의미 단위로 나눠 커밋하고 https://github.com/gyeomyi/hanroro 에 push까지 마쳐라.
- push 후 원격 저장소를 다시 확인해서 실제로 반영됐는지 검증해라.

## 5-1. .gitignore 보완
- 기존 .gitignore(.mcp.json, .claude/, .agents/, skills-lock.json 처리돼 있음)는 
  건드리지 말고, 이번 작업 중 새로 생긴 불필요한 파일(빌드 캐시, OS/IDE 메타파일, 
  로그, 스크린샷/디버그 산출물, .env 등)이 있으면 항목만 추가해라.
- 실수로 이미 커밋된 게 있으면 git rm --cached로 추적만 해제하고 실제 파일은 지우지 마라.

## 5-2. 안 쓰는 것 .gitignore 처리
- 그동안(초기 리디자인 + 이번 수정) 작업하면서 더 이상 안 쓰게 된 파일들을 찾아라:
  - 참조되지 않는 CSS/JS 파일, img/의 미사용 이미지
  - 옛 리디자인 이전 버전의 잔재, 목적 다한 임시/스크래치 파일
- 찾은 것들은 삭제하지 말고 .gitignore에 등록해서 추적에서만 빼라. 
  실제 파일은 그대로 남겨둬라(나중에 필요할 수 있으니 보존).
- 이미 git에 커밋돼 추적 중인 파일이면 git rm --cached로 추적만 해제하고 
  로컬 파일은 지우지 마라.
- 어떤 파일을 왜 안 쓴다고 판단했는지 DECISIONS.md에 목록으로 남겨라.

## 6. 완료 기준
- 각 항목은 "코드를 고쳤다"가 아니라 "실제로 재현 테스트를 통과했다"일 때만 완료로 
  표시해라. 완료 보고에는 무엇이 왜 안 됐었는지(원인), 어떻게 고쳤는지, 
  어떤 방법으로 실제 동작을 검증했는지를 항목별로 명시해라.
- 지속성 규칙: 작업 중 컨텍스트가 차거나 세션이 끊길 수 있다. 조기 종료하지 말고, 
  끊기기 전엔 PROGRESS.md에 상태를 남기고 자주 커밋해라.