# DECISIONS

spec.md 실행 중의 판단 기록. 짧게, 이유 위주로.

## P0 — 현황 평가 (2026.08.02)

### 유지 (이미 좋다, 손대면 손해)
- **2테마 CSS 변수 체계**(밤/낮) — 두 테마가 같은 변수 집합을 갖고 WCAG AA 검증까지 끝났다.
  spec의 "네이비 × 자몽/살구" 요구와 정확히 같은 축이다. 새로 짤 이유가 없다.
- **`.manuscript` 원고지 그리드** — 국문학도라는 정체성을 이미지 없이 CSS로만 표현. 이 사이트의 시그니처.
- **앨범별 세계(`album.css`)** — spec이 "예: 앨범별 컬러 테마 전환"으로 제안한 걸 이미 갖고 있다.
  실제 표지에서 뽑은 색이라 근거도 있다.
- **방명록 + Supabase 보안 구조** — RLS·컬럼 권한·bcrypt·브루트포스 제한까지 되어 있다.
  spec의 Community 요구를 이미 충족. 손대면 보안만 깨진다.
- **OG 메타 7페이지 · favicon · `prefers-reduced-motion` · skip 링크** — 품질 기준 이미 통과.

### 개선
- **히어로** — 정적이다. spec은 "책의 첫 장 / 이륙 직전"의 감각을 요구한다.
  → 스크롤 패럴랙스 + 글자 등장 연출을 얹는다. 구조는 유지.
- **디스코그래피** — 22장이 세 덩어리로 흩어져 있고 정적이다.
  → 필터/연도 익스플로러를 얹어 하나로 훑을 수 있게. 기존 카드 마크업은 재사용.
- **질감** — spec이 명시한 "필름 그레인/종이 질감"이 없다.
  → SVG `feTurbulence` 한 겹. 이미지 파일 없이.
- **푸터** — 벅스/유튜브/인스타뿐. 스포티파이·애플뮤직·멜론 공식 링크 추가.

### 신규 (spec 요구, 현재 없음)
- Listen(공식 임베드), 문장(인용 큐레이션), Live/Schedule, Gallery, 이스터에그.

### 폐기
- 없음. 현재 페이지에 죽은 코드나 틀린 정보가 없다. 덜어낼 게 아니라 얹을 게 있는 상태다.

---

## P1 — 질감과 히어로

- **그레인은 SVG `feTurbulence` data URI 한 겹.** 이미지 파일을 새로 넣지 않는다.
  `body::after`에 `position:fixed` + `pointer-events:none`로 화면 전체에 깔고,
  `mix-blend-mode`는 쓰지 않는다(사파리에서 스크롤 성능이 떨어진다). 불투명도만으로 조절.
  → **낮 테마는 종이 결, 밤 테마는 필름 그레인**으로 세기를 다르게 잡는다.
- **히어로 패럴랙스는 CSS만.** 스크롤 이벤트로 `transform`을 만지면 프레임을 갉아먹는다.
  배경은 `background-attachment:fixed`가 모바일에서 깨지므로 쓰지 않고,
  JS `scroll` 리스너 하나에서 CSS 변수 `--sy`만 갱신해 여러 레이어가 그 변수를 쓴다.
  `prefers-reduced-motion`이면 리스너를 아예 붙이지 않는다.

## P2 — 디스코그래피 익스플로러

- **기존 목록을 지우고 JS로 렌더하지 않는다.** HTML에 22장이 이미 다 적혀 있고 SEO·noscript에 유리하다.
  필터는 **이미 있는 DOM에 `hidden`을 토글**하는 30줄짜리로 끝낸다.
  → 데이터를 JS 배열로 옮기면 코드가 늘고 검색엔진이 못 읽는다. 얻는 게 없다.
- 필터 축은 **종류(EP/싱글/OST) + 연도**. 검색창은 22장에 과하다.

## P3 — Listen

- **YouTube iframe을 그대로 박지 않는다.** 임베드 하나가 ~800KB에 쿠키까지 싣는다.
  → **파사드**(표지 + 재생 버튼)를 깔고 클릭할 때만 iframe을 만든다. `youtube-nocookie.com` 사용.
  Lighthouse 90+ 목표와 직결된다.
- 썸네일은 `i.ytimg.com`에서 받는다 — 공식 CDN이므로 스크래핑이 아니다.

## P4 — 문장

- **가사 전문은 넣지 않는다.** 곡 제목·앨범 소개문·인터뷰 발언만 쓴다.
  인터뷰 인용은 매체와 연도를 반드시 붙인다.

## P6 — Gallery

- **사진을 새로 긁어오지 않는다.** 보유한 `img/cover-*.jpg`·`disc-*.jpg` 22장으로 표지 월을 만든다.
  공식 배포 아트워크이고 각각 벅스 원본으로 연결된다. 인물 사진이 필요한 자리는 TODO로 남긴다.

- 스포티파이 아티스트 id `5wVJpXzuKV6Xj7Yhsf2uYx` — 검색으로 찾고 실제 페이지를 열어
  이름(HANRORO)과 대표곡(0+0 · Landing in Love · Let Me Love My Youth)까지 확인했다.
  아티스트 임베드는 파사드 없이 `loading="lazy"` iframe 하나로 둔다 — 그 자리까지
  내려와야 불러오므로 초기 로드에 영향이 없고, 코드도 늘지 않는다(네이티브 속성).
- 영상 여섯 개는 **레포 안에 이미 있던 id만 썼다.** 새 id를 추측해 넣으면
  하나만 죽어도 재생이 안 되는데 확인할 방법이 없다.
