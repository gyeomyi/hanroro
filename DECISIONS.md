# DECISIONS

spec.md 실행 중의 판단 기록. 짧게, 이유 위주로.

## P0 — 현황 평가 (2026.08.02)

### 유지 (이미 좋다, 손대면 손해)
- **2테마 CSS 변수 체계**(밤/낮) — 두 테마가 같은 변수 집합을 갖고 WCAG AA 검증까지 끝났다.
  spec의 "네이비 × 자몽/살구" 요구와 정확히 같은 축이다. 새로 짤 이유가 없다.
- **`.manuscript` 원고지 그리드** — 국문학도라는 정체성을 이미지 없이 CSS로만 표현. 이 사이트의 시그니처.
- **앨범별 세계(`album.css`)** — spec이 "예: 앨범별 컬러 테마 전환"으로 제안한 걸 이미 갖고 있다.
  실제 표지에서 뽑은 색이라 근거도 있다.
- **방명록 + Supabase 보안 구조** — RLS·컬럼 권한·bcrypt·브루트포스 제한까지 되어 있다.
  spec의 Community 요구를 이미 충족. 손대면 보안만 깨진다.
- **OG 메타 7페이지 · favicon · `prefers-reduced-motion` · skip 링크** — 품질 기준 이미 통과.

### 개선
- **히어로** — 정적이다. spec은 "책의 첫 장 / 이륙 직전"의 감각을 요구한다.
  → 스크롤 패럴랙스 + 글자 등장 연출을 얹는다. 구조는 유지.
- **디스코그래피** — 22장이 세 덩어리로 흩어져 있고 정적이다.
  → 필터/연도 익스플로러를 얹어 하나로 훑을 수 있게. 기존 카드 마크업은 재사용.
- **질감** — spec이 명시한 "필름 그레인/종이 질감"이 없다.
  → SVG `feTurbulence` 한 겹. 이미지 파일 없이.
- **푸터** — 벅스/유튜브/인스타뿐. 스포티파이·애플뮤직·멜론 공식 링크 추가.

### 신규 (spec 요구, 현재 없음)
- Listen(공식 임베드), 문장(인용 큐레이션), Live/Schedule, Gallery, 이스터에그.

### 폐기
- 없음. 현재 페이지에 죽은 코드나 틀린 정보가 없다. 덜어낼 게 아니라 얹을 게 있는 상태다.

---

## P1 — 질감과 히어로

- **그레인은 SVG `feTurbulence` data URI 한 겹.** 이미지 파일을 새로 넣지 않는다.
  `body::after`에 `position:fixed` + `pointer-events:none`로 화면 전체에 깔고,
  `mix-blend-mode`는 쓰지 않는다(사파리에서 스크롤 성능이 떨어진다). 불투명도만으로 조절.
  → **낮 테마는 종이 결, 밤 테마는 필름 그레인**으로 세기를 다르게 잡는다.
- **히어로 패럴랙스는 CSS만.** 스크롤 이벤트로 `transform`을 만지면 프레임을 갉아먹는다.
  배경은 `background-attachment:fixed`가 모바일에서 깨지므로 쓰지 않고,
  JS `scroll` 리스너 하나에서 CSS 변수 `--sy`만 갱신해 여러 레이어가 그 변수를 쓴다.
  `prefers-reduced-motion`이면 리스너를 아예 붙이지 않는다.

## P2 — 디스코그래피 익스플로러

- **기존 목록을 지우고 JS로 렌더하지 않는다.** HTML에 22장이 이미 다 적혀 있고 SEO·noscript에 유리하다.
  필터는 **이미 있는 DOM에 `hidden`을 토글**하는 30줄짜리로 끝낸다.
  → 데이터를 JS 배열로 옮기면 코드가 늘고 검색엔진이 못 읽는다. 얻는 게 없다.
- 필터 축은 **종류(EP/싱글/OST) + 연도**. 검색창은 22장에 과하다.

## P3 — Listen

- **YouTube iframe을 그대로 박지 않는다.** 임베드 하나가 ~800KB에 쿠키까지 싣는다.
  → **파사드**(표지 + 재생 버튼)를 깔고 클릭할 때만 iframe을 만든다. `youtube-nocookie.com` 사용.
  Lighthouse 90+ 목표와 직결된다.
- 썸네일은 `i.ytimg.com`에서 받는다 — 공식 CDN이므로 스크래핑이 아니다.

## P4 — 문장

- **가사 전문은 넣지 않는다.** 곡 제목·앨범 소개문·인터뷰 발언만 쓴다.
  인터뷰 인용은 매체와 연도를 반드시 붙인다.
- **인용문은 전부 실제 기사에서 확인하고 옮겼다.** 실존 인물의 말이라 지어내면 안 된다.
  - GQ 코리아 2026.04.23 — "다정은 언제든지 세상을 바꿀 수 있는 힘이 있다고 생각해요" (기사 제목 그대로)
  - 위버스 매거진 — "저에게 사랑은 일종의 '연명할 수 있는 이유' 같은 느낌이에요" / "저도 사람이니까…"
  - 빌보드 코리아 2026.04.07 — "제가 지금 당장 할 수 있는 건 거창한 무언가보다…"
  - 하퍼스 바자 코리아 2025.08.14 — "글을 쓸 때와 노래 가사를 적을 때의 마음가짐은 비슷하다" (프로필 인용으로)
  네 인용 모두 `figcaption`에서 원문 기사로 링크된다.
- **프로필의 '다정' 인용은 문장 섹션으로 옮겼다.** 한 페이지에 같은 말을 두 번 싣지 않는다.

- 스포티파이 아티스트 id `5wVJpXzuKV6Xj7Yhsf2uYx` — 검색으로 찾고 실제 페이지를 열어
  이름(HANRORO)과 대표곡(0+0 · Landing in Love · Let Me Love My Youth)까지 확인했다.
  아티스트 임베드는 파사드 없이 `loading="lazy"` iframe 하나로 둔다 — 그 자리까지
  내려와야 불러오므로 초기 로드에 영향이 없고, 코드도 늘지 않는다(네이티브 속성).
- 영상 여섯 개는 **레포 안에 이미 있던 id만 썼다.** 새 id를 추측해 넣으면
  하나만 죽어도 재생이 안 되는데 확인할 방법이 없다.

## P5 — 무대 (Live/Schedule)

- **셋리스트를 지어내지 않는다.** 확인된 곡 목록이 없으므로 `<details>` 구조만 두고,
  안에는 "공연 뒤 공식 채널에서 확인해 채운다"는 안내를 넣었다. 팬 페이지가 틀린 정보를
  퍼뜨리는 쪽이 되면 안 된다. 코드에는 `TODO(에셋)` 주석으로 남겼다.
- 여닫기는 **`<details>`/`<summary>` 그대로**. JS도 aria 속성도 필요 없다 — 브라우저가 다 한다.
- 남은 날(D-4)만 `js/live.js`가 붙인다. 날짜 자체는 HTML에 적혀 있어 JS가 죽어도 보인다.
  `data-date`와 화면에 보이는 날짜는 **손으로 맞춰야 한다** — 어긋나면 D-day만 틀린다.

## P6 — Gallery

- **사진을 새로 긁어오지 않는다.** 보유한 `img/cover-*.jpg`·`disc-*.jpg` 22장으로 표지 월을 만든다.
  공식 배포 아트워크이고 각각 벅스 원본으로 연결된다. 인물 사진이 필요한 자리는 TODO로 남긴다.
- **가장 최근 발매작만 2×2로 키웠다.** 위계도 생기고, 스물두 장이 마지막 줄에 한 장만
  덩그러니 남아 깨져 보이던 것도 같이 풀린다.
- 설명(제목·발매일)은 **DOM에 늘 있고 눈으로만 가린다** — 스크린리더는 언제나 읽는다.
  `@media (hover:none)`에서는 아예 띄워 둔다. 손가락으로 보는 화면엔 hover가 없다.

## P7 — 숨은 문장 (1111)

- **여는 길을 둘로 뒀다.** `1111` 타이핑과 `0+0=∞` 네 번 두드리기.
  휴대폰에는 키보드가 없어서, 타이핑 하나만 두면 방문자 절반은 영영 못 찾는다.
- 안내 문구는 두지 않는다(숨은 것이니까). 대신 `.formula`의 커서만 포인터로 바꿔
  "눌러볼 수 있다"는 최소한의 힌트를 남겼다.
- 6초 뒤 자동으로 닫힌다 — 닫는 법을 모르는 사람이 갇히지 않게. `role="status"`라
  스크린리더도 열리는 순간 읽는다.
- `keydown` 처리에서 `e.target`이 늘 Element인 건 아니다(document·window로 올 수 있다).
  `closest`를 그냥 부르면 그 자리에서 죽으므로 옵셔널 체이닝으로 막았다.

## P8 — 최종 감사

- **대비는 자동으로 쟀다.** 페이지의 모든 글자 요소를 훑어 유효 배경과의 비율을 계산.
  밤·낮 두 테마 전부 통과. 새로 걸린 셋을 고쳤다:
  `.pl-txt span`(4.40 → --text-dim), 고른 곡의 `.pl-no`(자몽 4.38 → 살구),
  `.art-cap` 장막(임의의 표지 위에 얹히므로 글자 구간에서 충분히 짙게).
  그라디언트로 채운 요소(`.btn`·`.badge-solid`·`.chip.is-on`)는 computed 값으로
  바탕을 읽을 수 없어 자동 검사에서 뺐다 — 이들은 `--btn-ink` 규칙으로 이미 맞춰져 있다.
- **가로 넘침은 390 / 768 / 1024에서 확인.** `scrollWidth == clientWidth`.
  `.hero-bg`·`.club-glow`가 부모 밖으로 나가지만 둘 다 `overflow:hidden` 안이다.
- 공연 카드가 좁은 화면에서 오른쪽 배지가 잘려 760px 이하 배치를 따로 잡았다.
- 스포티파이 임베드는 `theme=0`이라 낮 테마에서도 어둡다. 플레이어 패널로 읽히므로 그대로 둔다.

## P8-2 — Lighthouse (측정으로 원인을 찾은 기록)

첫 측정: 성능 55 / 접근성 97 / 권장사항 100 / SEO 100. FCP 12.7s.

- **접근성 97 → 100**: `<main>` 랜드마크가 없었고(7페이지 전부 추가), 푸터 열 제목이
  `<h4>`라 바로 앞 `.cta`의 `<h2>`에서 한 단계 건너뛰고 있었다 → `<h3>`.
- **성능은 짐작하지 말고 하나씩 빼서 쟀다.**
  - AOS를 통째로 빼 봤다 → **55 그대로.** 범인이 아니었다. 7개 페이지를 갈아엎을 뻔했다.
  - 배경 preload를 빼 봤다 → 55 그대로.
  - **구글 폰트 스타일시트를 빼 봤다 → 88, FCP 2.0s.** 범인은 이것 하나였다.
- 한글 웹폰트는 서브셋이 수십 개로 쪼개져 **스타일시트만 78KB**다. 이게 렌더를 막는
  자리에 있으면 첫 페인트가 통째로 밀린다.
  → `rel="preload" as="style"` + `onload`로 승격. 받는 동안 글자는 CSS 폰트 스택에
  이미 적혀 있던 대체 글꼴(맑은 고딕·나눔명조)로 먼저 보인다. `display=swap`이라
  어차피 한 번은 바뀌던 것이고, **그 시점을 앞당겼을 뿐 새로 생긴 깜빡임이 아니다.**
  `<noscript>` 대비도 함께 뒀다.
- 안 쓰는 굵기 정리: CSS 어디에도 `font-weight:600`이 없는데 `IBM+Plex+Sans+KR`에서
  받고 있었다. 한글 폰트는 굵기 하나가 곧 파일 수십 개다.
- `img/background.jpg` 2940×1852(447KB) → 1600×1008(161KB). 히어로는 장막(0.72~0.95)과
  `grayscale(0.35)`을 뒤집어쓰므로 원본 해상도가 화면에 드러나지 않는다.
  og:image로도 계속 쓰인다(권장 1200×630보다 크다).
- 접힌 아래 사진(`profile.jpg`·`card.jpg`)에 `loading="lazy"` + 실제 크기 속성.
- `vercel.json`에 캐시 헤더. 정적 파일이라 재방문에서 다시 받을 이유가 없다.

**최종: 성능 90 / 접근성 100 / 권장사항 100 / SEO 100** (FCP 2.1s · LCP 3.2s · CLS 0).

---

# FIX.md 실행 (2026.08.02)

## F3 — 주력 스트리밍 플랫폼을 유튜브 뮤직으로

**근거 (와이즈앱·리테일 데이터, 2026년 6월 국내 음악 앱 월간 이용자):**

| 순위 | 플랫폼 | MAU | 비고 |
|---|---|---|---|
| 1 | **유튜브 뮤직** | 949만 | 2026 상반기 점유율 42% |
| 2 | 스포티파이 | 622만 | 전년 대비 +60%, 출시 이래 최대 |
| 3 | 멜론 | 593만 | 전년 대비 −44만. 처음으로 3위로 밀림 |
| 4 | 지니 237만 · 플로 168만 · 사운드클라우드 127만 · 바이브 60만 | | |

- 국내 음원 시장에서 **외산이 1·2위를 차지한 첫 통계**다(2026.06).
- **벅스는 이 표에 이름조차 없다.** 그런데 이 사이트는 벅스를 가장 앞에 두고 있었다 —
  푸터 첫 줄도, 듣기 섹션의 "전곡은 …에서"도 벅스였다. 실제 팬이 쓰는 곳과 어긋난다.
- 출처: [테크42](https://www.tech42.co.kr/), [한국경제 2026.07.28](https://www.hankyung.com/article/202607280789i),
  [매드클럽 와이즈앱 인사이트](http://www.madclub.co.kr/news/articleView.html?idxno=14419)

**바꾼 것**
- 듣기 섹션 아래에 `.plat` 한 줄 추가 — **유튜브 뮤직만 버튼(`.btn`)으로 세우고**
  스포티파이·멜론·애플뮤직·벅스는 그 옆 보조 링크로.
- 스포티파이 임베드는 **남긴다.** 유튜브 뮤직은 웹 임베드가 없고, 전곡 목록을
  페이지 안에서 보여줄 수 있는 건 이것뿐이다. 다만 역할을 '미리듣기'로 낮췄다
  (`음원으로` → `먼저 30초만`).
- 푸터 Listen 칼럼 순서를 이용자 수대로: YouTube Music · Spotify · 멜론 · Apple Music · 벅스뮤직.
  앨범 5장의 한 줄 푸터도 `벅스뮤직·YouTube` → `유튜브 뮤직·벅스뮤직`.
- **벅스 앨범 링크(`/album/<id>`)는 그대로 둔다.** 그건 듣는 곳이 아니라 디스코그래피
  출처이고, 발매일·수록곡·기획사가 한자리에 있는 곳은 아직 벅스뿐이다.

**링크는 전부 열어서 확인했다** (`og:title`이나 oEmbed로 '한로로'인지 대조):
- 유튜브 뮤직 `music.youtube.com/channel/UCrDa_5OU-rhvXqWlPx5hgKQ` — `<title>한로로`
- 스포티파이 `5wVJpXzuKV6Xj7Yhsf2uYx` — oEmbed `Spotify Embed: HANRORO`
- **멜론 `artistId=3080810`** — `og:title="한로로"`.
  CLAUDE.md에 "id를 확인 못 해 못 넣었다"로 남아 있던 TODO를 이걸로 닫는다
- 애플뮤직 `1613668993` — `<title>한로로 - Apple Music`

## F1·F2 곁가지 — 390px 가로 넘침

`.disc-list`가 `minmax(430px, 1fr)`이라 화면이 430px보다 좁아도 칸이 줄지 않았다.
390px에서 63px 넘침. `minmax(min(430px,100%), 1fr)`로 고침. P8에서 "넘침 없음"으로
적어둔 건 이 목록이 커지기 전 이야기였던 듯하다.

## F4 — 카피 손보기 (전 / 후)

기준: **"한 권의 책" 톤은 그대로 두고, 틀린 문장·홍보문 말투·낡은 관용구만 걷어낸다.**
밈체·신조어는 쓰지 않았다. 인용문(인터뷰·라이너노트)은 **한 글자도 건드리지 않았다** —
실존 인물이 실제로 한 말이다.

| 자리 | 전 | 후 | 왜 |
|---|---|---|---|
| 음반 · EP2 | 무너진 집**을** 자모**를** 떼어 'ㅈㅣㅂ'이라 적었습니다 | 무너진 집**이라** 자모를 하나씩 떼어 … | 목적격 조사가 겹친 **비문** |
| 음반 · EP1 | …보는 시선에도**,** 비행하겠다는 | …보는 시선에도 비행하겠다는 | 쉼표가 말놀이의 호흡을 끊었다 |
| 프로필 | 작업 순서가 남다릅니다 | 순서가 거꾸로입니다 | '남다릅니다'는 보도자료 말투. 짧게 단정하는 쪽이 이 사람답다 |
| 프로필 | …느낌이 **충격**이었고, 그런 **충격**을 주고 싶다고 | 노래 안으로 걸어 들어가 그 상황에서 말을 거는 것 같았다고 … 그런 **순간**을 만들어주고 싶다고 | 한 문장에 같은 단어 두 번 |
| 자몽살구클럽 | 같은 세계관을 나눠 갖는, **흔치 않은 시도입니다** | 같은 세계를 나눠 갖습니다 | 스스로 대단하다고 말하지 않는다. 사실만 두면 충분하다 |
| 음반 | EP 세 장과 싱글 열다섯 장, **그리고** OST 네 편 | EP 세 장, 싱글 열다섯 장, OST 네 편 | 번역투 접속사 |
| 음반 | 표지를 누르면 …**전체를 볼 수 있습니다** | 표지를 누르면 …**전부 나옵니다** | 주어가 어긋난 문장이었다 |
| 거르기 | **그 해**에는 그 종류의 발매가 없습니다 | **그해**에는 그런 발매가 없습니다 | 띄어쓰기 오류 |
| 듣기 | 재생을 누르기 전에는 표지 그림만 놓아둔 자리이고 | **곡을 고르면 그 자리에서 바로 재생되고**, 그 전까지는 표지 그림뿐이라 | F1에서 동작이 바뀌었다. 설명이 실제와 어긋나면 안 된다 |
| 행보 | 후보에 그친 해도 **그때의 자리라** | 상을 놓친 해도 **그 자리에 있었던 건 맞으니** | 무슨 뜻인지 읽히지 않았다 |
| 행보 | 디지털·스트리밍 부문을 동시 **석권**했습니다 | 디지털과 스트리밍 부문에서 **나란히 1위에 올랐습니다** | '석권'은 스포츠 기사 말투 |
| 행보 | **전 해**에는 같은 부문 후보에 | **한 해 전에는** 같은 부문 후보에 | 어색한 띄어쓰기 |
| 행보 | **첫 단독 공연에서 2년 만에** 객석이 | **첫 단독 공연으로부터 2년,** 객석이 | '공연에서 2년 만에'는 말이 안 된다 |
| 무대·행보 | 음악으로 남겨온 **발자취를 관객과 함께 따라가는** | **그동안 낸 노래를 처음부터 차례로 짚어가는** | '발자취를 따라가는'은 닳은 표현. 실제로 무엇을 하는지로 바꿨다 |
| 더 보기 | 한로로를 **만날 수 있는 다른 곳들** | **이 페이지 밖에서 한로로를 만나는 자리들** | 브로슈어 말투 |
| 더 보기 첫 칸 | 벅스뮤직 — 전체 디스코그래피를 한 곳에서 | **YouTube Music** — 앨범도 싱글도 OST도, 전곡이 여기 다 있습니다 | F3의 판단을 링크 그리드에도 반영 |

**손대지 않은 것** — 히어로 리드("가사를 쓰기 전에 산문을 먼저 씁니다"), 방명록 CTA
("쓰는 사람에게는, 읽는 사람이 필요합니다"), `0+0=∞`, 앨범 페이지 본문, 셋리스트·갤러리의
"안 싣는 이유" 문단. 전부 이 사이트의 논지 그 자체라 바꿀 이유가 없다.
