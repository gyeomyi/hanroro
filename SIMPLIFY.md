너는 gyeomyi/hanroro 레포의 웹페이지를 간소화하고 말투를 트렌디하게 바꾼다. 
나는 개입하지 않는다. 질문하지 말고 네가 직접 판단해서 끝까지 완료해라. 
시작 전 CLAUDE.md를 반드시 읽어라.

## 0. 먼저 할 일
1. 헤드리스 크롬으로 전 페이지(index, 5개 album, guestbook)를 실제로 띄워서 
   현재 상태를 눈으로 확인해라. 스크린샷으로 지금 상태를 남겨라(전/후 비교용).
2. 이번 작업으로 빠지거나 바뀌는 것들을 SIMPLIFY-DECISIONS.md에 근거와 함께 기록해라.

## 1. 불필요한 요소 판단해서 제거/자연스럽게 수정
- 예시: 유튜브 영상을 재생하려면 먼저 어색한 실행 버튼을 눌러야 하는 구조라면, 
  왜 그렇게 돼 있는지 먼저 확인해라(파사드 방식은 성능 때문일 수 있다 — 
  CLAUDE.md나 이전 기록에 이유가 있는지 확인). 이유가 있으면 그 이유는 살리되 
  사용자 경험이 어색하지 않게 다듬고, 이유 없이 그냥 어색하기만 하면 제거해라.
- 그 외에도 눌러보면서 "굳이 있을 필요 없다" 싶은 버튼, 단계, 확인창, 
  장식 요소를 네가 판단해서 제거하거나 자연스럽게 바꿔라.
- 단, CLAUDE.md에 근거가 명시된 요소(원고지 그리드, 드롭캡, 앨범별 세계관, 
  0+0=∞ 모티프 등 정체성 장치)는 "불필요"로 판단하지 마라 — 이것들은 
  장식이 아니라 이 사이트의 정체성이다.

## 2. 필요하다 싶은 것 추가
- 네가 페이지를 둘러보면서 "이건 있는 게 낫겠다" 싶은 걸 판단해서 추가해라.
- 새로 추가하는 것도 기존 밤/낮 테마 변수 체계, 앨범별 컬러 체계를 그대로 따라야 한다. 
  하드코딩 색 금지, 새 변수는 두 테마 모두에 추가.

## 3. 말투를 트렌디하게
- 사이트 전체 카피(안내문구, 버튼 라벨, 빈 상태 문구, 에러 메시지 등)를 
  요즘 감각으로 다듬어라.
- 단, "한 권의 책" 컨셉과 문학적 톤(CLAUDE.md 디자인 컨셉 참고)은 깨지 마라. 
  트렌디 = 인터넷 밈체·과한 신조어가 아니라, 세련되고 자연스럽고 요즘 사람이 
  읽기에 촌스럽지 않은 문장이다. 격은 유지하되 낡은 표현만 걷어내라.
- 바꾼 문장은 전/후 비교로 SIMPLIFY-DECISIONS.md에 남겨라.

## 4. 어색한 공백 정리
- 헤드리스 크롬으로 실제 렌더링 화면을 보면서 불필요하게 뜬 여백, 
  섹션 간 리듬이 안 맞는 간격, 빈 것처럼 보이는 구간을 찾아 정리해라.
- 반응형 4단계(1100/900/760/480)에서 각각 다시 확인해라. 
  데스크톱에서 괜찮아도 모바일에서 어색할 수 있다.

## 5. 검증 (완료 기준)
- 각 변경은 "코드를 고쳤다"가 아니라 "헤드리스 크롬으로 실제 렌더링을 확인했다"일 
  때만 완료로 인정한다.
- 전 페이지, 반응형 4단계 전부 스크린샷으로 전/후 비교해라.
- Lighthouse 재측정해서 이전보다 나빠지지 않았는지 확인해라.

## 6. 커밋·푸시
- 작업이 끝나면 git remote -v로 origin이 https://github.com/gyeomyi/hanroro 로 
  잡혀 있는지 먼저 확인해라.
- 의미 단위로 커밋하고 push까지 한 뒤, GitHub 웹에서 실제로 반영됐는지 
  다시 확인해라. "커밋했다"는 보고만으로 끝내지 마라 — 이전에 push가 
  조용히 실패한 전례가 있다.
- 그동안 안 쓰게 된 파일은 삭제하지 말고 .gitignore에 등록만 해라.

## 6-1. 작업용 파일 정리
- spec.md, FIX.md, SIMPLIFY.md, PROGRESS.md는 프로젝트의 영구 문서가 아니라 
  이번 작업들을 위한 일회성 지시서/상태 파일이다. 작업 완료 후 .gitignore에 
  추가해서 커밋 대상에서 빼라 (이미 커밋된 게 있으면 git rm --cached).
- SIMPLIFY-DECISIONS.md 등 판단 근거 기록은 핵심만 요약해서 CLAUDE.md의 
  적절한 섹션(디자인 컨셉, 알고 둔 한계 등)에 합쳐 넣고, 원본 로그 파일은 
  커밋하지 말고 gitignore 처리해라.

## 7. 지속성 규칙
- 컨텍스트가 차거나 세션이 끊길 수 있다. 조기 종료하지 말고, 끊기기 전엔 
  PROGRESS.md에 상태를 남기고 자주 커밋해라.
- ponytail이 설치돼 있다면 이번 작업(간소화)엔 오히려 잘 맞는다 — 
  다만 CLAUDE.md에 근거가 명시된 정체성 장치는 "불필요한 코드"로 취급해 
  건드리지 마라.

## 완료 보고
- 무엇을 뺐고 왜(근거), 무엇을 추가했고 왜, 말투를 어떻게 바꿨는지(전/후 예시), 
  공백을 어떻게 정리했는지, Lighthouse 전/후 수치, 실제 커밋·push 확인 결과까지 
  전부 요약해서 보고해라.