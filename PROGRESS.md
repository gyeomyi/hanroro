# PROGRESS

spec.md 실행 기록. **새 세션은 CLAUDE.md → 이 파일 → DECISIONS.md 순으로 읽고 이어받을 것.**

## 지금까지 한 것
- P0 기준점 커밋 (`c591c65`) — 싱글·OST 표지 17장 + 메인 디스코 목록 확장
- P0 현황 평가 → DECISIONS.md에 유지/개선/폐기 기록
- P1 그레인·히어로 패럴랙스 (`810aa0e`)
- P2 음반 거르기 — 종류×연도 칩. 헤드리스 자체검사 11항목 통과 (`bd8340b`)
- P3 듣기 — 유튜브 파사드 6곡 + 스포티파이 임베드. 자체검사 12항목 통과 (`5e5268c`)
- P4 문장 — 실제 인터뷰 인용 4개(GQ·위버스·빌보드), 출처 기사 링크. 가사는 싣지 않음 (`ad62dc8`)
- P5 무대 — 다가오는 공연 2건 + D-day 배지 + 셋리스트 <details>(내용은 TODO) (`c985932`)
- P6 표지 — 보유 아트워크 22장 아트월. 최신작만 2×2. 공연 사진은 TODO로 남김 (`b23d3de`)
- P7 숨은 문장 1111 — 타이핑 / 0+0=∞ 네 번 두드리기. 자체검사 8항목 통과 (`f9b87b0`)
- P8 최종 감사 — 대비(밤·낮 전부 통과) · 반응형(390/768/1024 가로 넘침 없음) ·
  자체검사 29항목 통과 · 앨범 페이지 회귀 없음 · 푸터 스트리밍 링크 · OG 문구 갱신 (`0693a67`)
- P8-2 Lighthouse **90 / 100 / 100 / 100** — 원인은 렌더를 막던 구글 폰트 스타일시트
  하나였다(FCP 12.7s → 2.1s). AOS·배경 preload는 빼서 재 보니 무관했다

## 지금 하는 것
- FIX.md 실행 (F0~F5). 아래 표 참고

---

# FIX.md 실행 기록 (2026-08-02 시작)

## F0 현황 파악 ✅
- `main`이 `origin/main`보다 **12커밋 앞서 있다**(`09d10b9`…`bf26dd1` 미푸시). 충돌 없음, 브랜치 하나
- **배포본(`hanroro-eight.vercel.app`)은 12커밋 전 버전**이다. HTML 19KB에 섹션이
  `top/about/club/records/more` 다섯 개뿐 — `listen`·`words`·`live`·`gallery`가 통째로 없다

## 검증 도구 (이번에 만든 것)
`scratchpad/cdp.js` — 노드 24의 내장 `WebSocket`으로 CDP를 직접 물린다. 의존성 0.
```bash
chrome --headless=new --remote-debugging-port=9222 --user-data-dir=<임시> about:blank &
node cdp.js <url> <저장할 png> "<셀렉터1>|<셀렉터2>" <대기ms>     # 데스크톱
MOBILE=1 node cdp.js ...                                        # 390×844 터치
```
- ⚠️ **`dispatchEvent(new MouseEvent('click'))`로는 재생을 검증할 수 없다.** 합성 이벤트는
  `isTrusted:false`라 사용자 활성화를 주지 않고, 그러면 유튜브 오토플레이가 막혀
  **멀쩡한 코드도 '재생 안 됨'으로 보인다**(실제로 그렇게 오진했다). CDP `Input.dispatch*Event`만 진짜다
- ⚠️ **`--virtual-time-budget`은 `setTimeout`을 순간이동시킨다.** 타이머 뒤에 iframe을 만들면
  실제 네트워크가 도착하기 전에 브라우저가 종료돼 **`base.js`조차 안 받은 것처럼 보인다**
- ⚠️ 유튜브 iframe은 별도 프로세스(OOPIF)라 `Target.setAutoAttach`로 붙어야 네트워크가 보인다
- 좌표는 AOS 애니메이션 때문에 흔들린다 → 두 번 연속 같은 값이 나올 때까지 기다린 뒤 누른다

## F1 유튜브 재생 — 원인 확정
- **로컬은 6곡 전부 정상 재생된다.** 진짜 클릭으로 확인:
  `base.js` 로드 + `googlevideo.com/videoplayback` 요청 3~5건 + 스크린샷에 영상 프레임.
  데스크톱·모바일(390×844 터치) 둘 다
- 영상 id 6개 전부 살아 있고 임베드도 허용된다(oEmbed 200, IFrame API `onError` 없음)
- **그래서 "재생 안 됨"의 원인은 코드가 아니라 배포본이다** — 배포된 페이지엔 플레이어가 없다.
  거기 있는 유튜브 링크는 히어로의 옛 영상(`niazCi1AqqA`)뿐. → F5 푸시로 해소
- 다만 재현 중에 **진짜 UX 결함 하나**를 찾았다: 목록에서 곡을 눌러도 재생되지 않고
  파사드만 바뀐다. 모바일은 무대가 화면 밖이라 **아무 일도 안 일어난 것처럼 보인다** → 고칠 것

## F2 방명록 수정 — 원인 확정
- **서버(RPC)는 멀쩡하다.** anon 키 curl로 작성→수정→오답거부→읽기→삭제 전부 확인
- 프론트도 정상 경로는 통과한다(여러 줄·특수문자·틀린 비번 재시도·취소·재편집 전부 OK)
- 실제 결함 3가지:
  1. **내용이나 비밀번호가 비면 아무 반응 없이 무시된다**(`focus()`만 부른다).
     사용자에겐 "저장을 눌러도 아무 일도 안 난다" = 수정 기능이 죽은 것으로 보인다
  2. 두 글을 동시에 편집하면, 한쪽을 저장·취소하는 순간 목록을 다시 그려 **다른 쪽 입력이 날아간다**
  3. 비밀번호 칸에서 Enter를 쳐도 저장되지 않는다

## F1 고친 것
- 목록에서 곡을 고르면 **바로 재생**. 파사드를 다시 깔지 않는다 + 무대로 스크롤.
  안 쓰이게 된 `facade()` 제거
- 검증: CDP 진짜 클릭으로 데스크톱 6곡 전부 `media` 요청 발생, 모바일도 재생 화면 확인.
  selfcheck 재작성(파사드 유지 검사 2개 → 즉시 재생 검사 1개, 29 → 27항목) 통과

## F2 고친 것
- `complain(el, msg)` — 빈 칸이면 브라우저 기본 말풍선. 작성 폼 3자리 + 편집 2자리
- 편집은 한 번에 하나만 (다른 편집창이 열려 있으면 먼저 목록을 새로 그리고 `data-id`로 재탐색)
- 비밀번호 칸 Enter로 저장
- 검증: 헤드리스로 작성→빈 비번 안내→빈 내용 안내→Enter 저장→동시편집 1개→저장까지 전부 통과.
  검사에 쓴 방명록 글 12건은 `remove_guestbook`으로 전부 지웠다

## F3 고친 것
- 주력 = **유튜브 뮤직** (와이즈앱 2026.06 949만, 1위). 근거·출처는 DECISIONS.md
- `.plat` 한 줄 신설(유튜브 뮤직만 버튼) · 푸터 7페이지 순서 정리 · 더 보기 첫 칸 교체
- **멜론 artistId 3080810 확인** — 오래 남아 있던 TODO 해소
- 곁가지: `.disc-list`가 390px에서 63px 넘치던 것 수정(`minmax(min(430px,100%),1fr)`)

## F4 고친 것
- 비문 2 · 띄어쓰기 2 · 보도자료 말투 4 · 닳은 관용구 1 등 16곳. 전/후 표는 DECISIONS.md
- 인용문(인터뷰·라이너노트)은 한 글자도 건드리지 않았다

## 남은 것
| # | 작업 | 상태 |
|---|------|------|
| F0 | 로컬/원격 차이 파악 | ✅ |
| F1 | 유튜브 재생 재현·원인·수정·검증 | ✅ |
| F2 | 방명록 수정 재현·원인·수정·검증 | ✅ |
| F3 | 주력 스트리밍 플랫폼 재선정 | ✅ |
| F4 | 카피 다듬기 | ✅ |
| F5 | 커밋·푸시·원격 확인 | ⬜ |

## 최종 점검 결과
- selfcheck **PASS 27/27**
- 가로 넘침: 7개 페이지 × 390/768/1024/1280 전부 0
- 내부 앵커·페이지 링크 전부 존재, 스트리밍 링크 6개 전부 200 + 이름 대조

## 남은 것 (순서대로)
| # | 작업 | 상태 |
|---|------|------|
| P0 | 레포 파악 · 유지/개선/폐기 판단 · 문서 생성 | ✅ |
| P1 | 필름 그레인/종이 질감 레이어 + 히어로 재연출(이륙 직전) | ✅ |
| P2 | 디스코그래피 익스플로러 — 필터(EP/싱글/OST/연도) + 정렬 | ✅ |
| P3 | Listen 섹션 — YouTube 파사드 임베드 + 스트리밍 링크 | ✅ |
| P4 | 문장 섹션 — 짧은 인용 + 출처 표기 (가사 전문 금지) | ✅ |
| P5 | Live/Schedule — 예정 공연 + 셋리스트 구조 (데이터 없으면 TODO) | ✅ |
| P6 | Gallery — 보유 에셋 기반 비주얼 월 (스크래핑 금지) | ✅ |
| P7 | 이스터에그 — 1111 / 0+0=∞ | ✅ |
| P8 | 최종 감사 — 접근성·대비·반응형·OG·헤드리스 렌더 검증 | ✅ |
| P9 | README·CLAUDE.md 갱신 + 최종 보고 | ✅ |

## 남은 TODO (실제 에셋이 필요한 자리)
- **셋리스트** (`.setlist`) — 공연 뒤 공식 채널에서 확인해 채울 것. 지어내지 말 것
- **공연·화보 사진** (`.gallery` 아래) — 사용 허락을 받으면 사진 줄 추가
- **멜론 아티스트 링크** — id를 확인 못 해 푸터에 넣지 않았다
- **방명록 페이지네이션** — `limit(50)`에서 잘린다. 더 쌓이면 필요

## 재개 방법
```bash
python -m http.server 8000 --directory .
chrome --headless=new --disable-gpu --hide-scrollbars \
  --window-size=1280,2000 --virtual-time-budget=6000 \
  --screenshot=out.png "http://localhost:8000/index.html"
```
⚠️ `file://`로 열면 AOS가 안 돌아 화면이 통째로 빈다.
